---
phase: quick
plan: 260326-x0y
type: execute
wave: 1
depends_on: []
files_modified: [app/src/routes/root.tsx]
autonomous: true
requirements: [UAT-pod-selector-refresh]

must_haves:
  truths:
    - "After creating a pod via PodSelector dialog, the new pod appears in the AppBar dropdown without a page refresh"
    - "After creating a pod via HomeView and navigating to it, the new pod appears in the AppBar dropdown without a page refresh"
  artifacts:
    - path: "app/src/routes/root.tsx"
      provides: "PodSelector with pod list refresh on create and navigation"
      contains: "GetPodsForPlayer"
  key_links:
    - from: "handleCreatePod"
      to: "setPods"
      via: "GetPodsForPlayer re-fetch after PostPod succeeds"
      pattern: "GetPodsForPlayer.*then.*setPods"
    - from: "useEffect [podId]"
      to: "setPods"
      via: "Re-fetch when navigated to unknown pod"
      pattern: "GetPodsForPlayer.*then.*setPods"
---

<objective>
Fix AppBar PodSelector not showing newly created pods until hard refresh.

Purpose: Two UAT failures — pod created via PodSelector dialog (test 7) or HomeView (test 6) does not appear in AppBar dropdown until page refresh.
Output: Updated PodSelector component in root.tsx with two fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/src/routes/root.tsx
@app/src/routes/home/index.tsx (read-only — do NOT modify)
</context>

<interfaces>
<!-- From app/src/http.ts -->
GetPodsForPlayer(playerId: number): Promise<Pod[]>
PostPod(name: string): Promise<{ id: number }>

<!-- From app/src/types.ts -->
interface Pod { id: number; name: string; ... }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-fetch pods after PodSelector dialog creation and on navigation to unknown pod</name>
  <files>app/src/routes/root.tsx</files>
  <action>
In the PodSelector component in app/src/routes/root.tsx, make two changes:

1. In `handleCreatePod` (line 128-141), after `PostPod` succeeds and before `navigate`, re-fetch the pod list so the new pod appears in the dropdown immediately:
   ```
   const { id } = await PostPod(podName);
   await GetPodsForPlayer(playerId).then(setPods);
   setCreatePodOpen(false);
   setPodName("");
   setSubmitting(false);
   navigate(`/pod/${id}`);
   ```
   Move the `GetPodsForPlayer` call right after `PostPod` succeeds, before closing the dialog and navigating.

2. Add a second `useEffect` after the existing one (line 114-116) that watches for navigation to a pod not in the current list. This handles the HomeView creation path where PodSelector is never notified:
   ```
   useEffect(() => {
       if (podId && playerId && !pods.find(p => String(p.id) === podId)) {
           GetPodsForPlayer(playerId).then(setPods).catch(() => {});
       }
   }, [podId, playerId]);
   ```
   The deps intentionally omit `pods` to avoid re-triggering after the fetch resolves. The `pods.find` check inside the effect body is sufficient — it reads the current value at effect execution time and only fetches when the pod is genuinely missing.

Do NOT modify the existing initial fetch useEffect (line 114-116).
Do NOT modify home/index.tsx.
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <done>
    - PodSelector handleCreatePod re-fetches pods via GetPodsForPlayer before navigating
    - New useEffect watches podId and re-fetches pods when current podId is not in the pods list
    - TypeScript compiles without errors
    - Existing initial fetch effect unchanged
    - home/index.tsx unchanged
  </done>
</task>

</tasks>

<verification>
- `cd app && ./node_modules/.bin/tsc --noEmit` passes
- Manual: Create pod via PodSelector dialog -> new pod appears in dropdown immediately
- Manual: Create pod via HomeView -> navigate to pod -> new pod appears in AppBar dropdown
</verification>

<success_criteria>
AppBar PodSelector dropdown reflects newly created pods immediately after creation, whether created from the PodSelector dialog or from HomeView, without requiring a page refresh.
</success_criteria>

<output>
After completion, create `.planning/quick/260326-x0y-fix-appbar-pod-selector-not-updating-aft/260326-x0y-SUMMARY.md`
</output>
