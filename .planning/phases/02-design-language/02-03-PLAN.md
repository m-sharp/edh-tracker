---
phase: 02-design-language
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/routes/root.tsx
autonomous: true
gap_closure: true
requirements:
  - DSNG-03

must_haves:
  truths:
    - "AppBar title Typography uses the theme h6 font (Josefin Sans), not a hardcoded fontFamily override"
  artifacts:
    - path: "app/src/routes/root.tsx"
      provides: "AppBar title using theme typography"
      contains: "variant=\"h6\""
  key_links:
    - from: "app/src/routes/root.tsx"
      to: "app/src/theme.ts"
      via: "Typography variant h6 inherits theme font"
      pattern: "variant.*h6"
---

<objective>
Remove the hardcoded `fontFamily: "monospace"` override from the AppBar title Typography in root.tsx so the theme's h6 Josefin Sans font applies.

Purpose: Close the DSNG-03 gap identified in 02-VERIFICATION.md — the monospace override violates the "no hardcoded values where MUI provides a pattern" rule.
Output: root.tsx with theme-consistent AppBar typography.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-language/02-VERIFICATION.md
@app/src/routes/root.tsx
@app/src/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove monospace fontFamily override from AppBar title</name>
  <files>app/src/routes/root.tsx</files>
  <read_first>
    - app/src/routes/root.tsx
    - app/src/theme.ts
  </read_first>
  <action>
In `app/src/routes/root.tsx`, find the Typography component for the AppBar title (around line 73-85). The current sx prop is:

```tsx
sx={{
    mr: 2,
    display: "flex",
    fontFamily: "monospace",
    fontWeight: 700,
    letterSpacing: ".3rem",
}}
```

Remove ONLY the `fontFamily: "monospace",` line. Keep all other sx properties (`mr`, `display`, `fontWeight`, `letterSpacing`) exactly as they are. The theme already defines h6 as Josefin Sans — removing the override lets the theme token apply.

Do NOT change:
- Line 84: `style={{ textDecoration: "none", color: "white" }}` on the Link — this is a React Router Link, not MUI, so inline style is correct (per VERIFICATION.md).
- Line 49: `style={{ display: "flex", alignItems: "center", textDecoration: "none", color: "white" }}` on the other Link — same reason.
- Any other part of the file.

After the edit, the sx prop should be:

```tsx
sx={{
    mr: 2,
    display: "flex",
    fontWeight: 700,
    letterSpacing: ".3rem",
}}
```
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - grep for `fontFamily` in app/src/routes/root.tsx returns zero matches
    - grep for `fontWeight: 700` in app/src/routes/root.tsx returns exactly one match (confirming the rest of the sx prop is intact)
    - grep for `letterSpacing: ".3rem"` in app/src/routes/root.tsx returns exactly one match
    - grep for `variant="h6"` in app/src/routes/root.tsx returns exactly one match
    - `tsc --noEmit` exits cleanly with no errors
  </acceptance_criteria>
  <done>AppBar title Typography no longer overrides fontFamily — theme h6 Josefin Sans applies. DSNG-03 gap closed.</done>
</task>

</tasks>

<verification>
- `grep -c 'fontFamily' app/src/routes/root.tsx` returns `0`
- `grep -c 'fontWeight: 700' app/src/routes/root.tsx` returns `1`
- `grep -c 'variant="h6"' app/src/routes/root.tsx` returns `1`
- TypeScript compiles: `cd app && ./node_modules/.bin/tsc --noEmit` exits 0
</verification>

<success_criteria>
The AppBar title in root.tsx uses the theme's h6 typography (Josefin Sans) with no hardcoded fontFamily override. DSNG-03 is fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-language/02-03-SUMMARY.md`
</output>
