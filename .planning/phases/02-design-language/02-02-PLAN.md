---
phase: 02-design-language
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/routes/pod.tsx
autonomous: false
requirements:
  - DSNG-02

must_haves:
  truths:
    - "All 4 Pod tabs (Decks, Players, Games, Settings) are reachable at 375px viewport width"
    - "Pod name heading renders in Josefin Sans bold at 28px"
    - "DataGrid is readable on dark background without custom overrides"
    - "Save button in Settings tab says 'Save Pod Name' for clarity"
    - "Touch targets on buttons are at least 44px tall"
  artifacts:
    - path: "app/src/routes/pod.tsx"
      provides: "Mobile-ready Pod view with scrollable tabs"
      contains: "scrollable"
  key_links:
    - from: "app/src/routes/pod.tsx"
      to: "app/src/theme.ts"
      via: "MUI ThemeProvider context (implicit — no direct import needed)"
      pattern: "variant=\"scrollable\""
---

<objective>
Apply mobile-specific fixes to the Pod view (the Phase 2 validation target) and verify the complete design system works at phone viewport width.

Purpose: DSNG-02 requires mobile usability verification. The Pod view is the representative view (D-16) — Tabs must be scrollable at 375px, button labels must be clear, and the dark theme must render correctly on all Pod view components.

Output: Updated pod.tsx with scrollable Tabs and clearer button label; human-verified mobile rendering at 375px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-language/02-CONTEXT.md
@.planning/phases/02-design-language/02-UI-SPEC.md
@.planning/phases/02-design-language/02-01-SUMMARY.md

<interfaces>
<!-- Pod view current Tabs component (line 72): -->
```typescript
<Tabs value={tab} onChange={(_, v) => setTab(v)} sx={{ mb: 2 }}>
```

<!-- Pod Settings tab Save button (line 318): -->
```typescript
<Button variant="contained" onClick={handleSaveName}>Save</Button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scrollable Tabs and rename Save button in Pod view</name>
  <files>app/src/routes/pod.tsx</files>
  <read_first>
    - app/src/routes/pod.tsx (full file — understand Tabs location and Settings tab Save button)
    - .planning/phases/02-design-language/02-UI-SPEC.md (Mobile Contract section — Tabs requirement; Copywriting Contract — button labels)
    - .planning/phases/02-design-language/02-RESEARCH.md (Pattern 4 — pod.tsx Tabs fix; pod.tsx Settings button label fix)
  </read_first>
  <action>
    **Tabs scrollable fix** (per UI-SPEC Mobile Contract):
    On the `<Tabs>` component (currently at line 72), add two props:
    - `variant="scrollable"`
    - `scrollButtons="auto"`

    The full Tabs element becomes:
    ```typescript
    <Tabs value={tab} onChange={(_, v) => setTab(v)} sx={{ mb: 2 }} variant="scrollable" scrollButtons="auto">
    ```

    **Button label rename** (per UI-SPEC Copywriting Contract):
    In the PodSettingsTab component, change the Save button label from `Save` to `Save Pod Name`:
    ```typescript
    <Button variant="contained" onClick={handleSaveName}>Save Pod Name</Button>
    ```

    Do NOT make any other changes to pod.tsx. Do not add DataGrid overrides (D-15 — auto-adapts). Do not change DataGrid height or column definitions — those are Phase 3 concerns.
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/routes/pod.tsx` contains `variant="scrollable"`
    - `app/src/routes/pod.tsx` contains `scrollButtons="auto"`
    - `app/src/routes/pod.tsx` contains `Save Pod Name`
    - `app/src/routes/pod.tsx` does NOT contain a standalone `>Save</Button>` on the handleSaveName button (the old label)
    - TypeScript compiles cleanly (exit 0)
  </acceptance_criteria>
  <done>Pod view Tabs are scrollable at narrow viewports; Settings Save button has descriptive label; TypeScript compiles cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Pod view at 375px mobile viewport</name>
  <files>app/src/routes/pod.tsx</files>
  <action>
    Human visually verifies the complete dark theme + mobile layout on the Pod view at 375px viewport width. This is the Phase 2 validation target (D-16). No code changes — verification only.

    What was built: Complete MUI dark theme system applied globally (Plan 01) plus Pod view mobile fixes (Task 1 above).

    Verification steps:
    1. Start the dev server: `cd app && npm start`
    2. Open the Pod view in Chrome (navigate to a pod page, e.g., `/pod/1`)
    3. Open DevTools (F12) and toggle device toolbar (Ctrl+Shift+M)
    4. Set viewport to 375px width (iPhone SE preset)
    5. Verify each item:

    **Background and colors:**
    - Page background is near-black (#0f1117), NOT white or gradient
    - AppBar is dark navy (#1a1a2e), NOT gold
    - Primary buttons ("New Game", "Save Pod Name") are gold (#c9a227)
    - Text is off-white (#e8edf5), readable without zooming

    **Typography:**
    - Pod name heading uses Josefin Sans (inspect the h4 element — computed font-family should show "Josefin Sans")
    - Body text uses Roboto

    **Tabs:**
    - All 4 tabs (Decks, Players, Games, Settings) are reachable — scroll arrows appear if needed
    - Tabs are not clipped or overlapping

    **DataGrid:**
    - DataGrid renders with dark background (auto-adapted from theme)
    - Column headers and cell text are readable

    **Touch targets:**
    - Buttons have adequate touch target size (visually at least ~44px tall)
    - Tab items are tappable without precision

    **No artifacts:**
    - No white flash on initial load
    - No gradient banding visible
    - No unstyled/white components
  </action>
  <verify>Human confirms Pod view passes all visual checks at 375px viewport</verify>
  <acceptance_criteria>
    - Human types "approved" after verifying all visual checks pass
    - Dark theme renders on Pod view (background, AppBar, buttons, text)
    - All 4 tabs reachable at 375px
    - No visual artifacts (white flash, gradient, unstyled components)
  </acceptance_criteria>
  <done>Human has verified Pod view at 375px viewport — dark theme, mobile layout, typography, and touch targets all pass</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit` exits 0
2. Pod view renders correctly at 375px viewport with dark theme
3. All tabs reachable via scroll at narrow width
4. Human has verified mobile usability
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- Pod view Tabs are scrollable (variant="scrollable" present)
- Save button reads "Save Pod Name"
- Human confirms: dark theme renders correctly on Pod view at 375px
- Human confirms: all tabs reachable, text readable, touch targets adequate
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-language/02-02-SUMMARY.md`
</output>
