---
phase: 02-design-language
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/theme.ts
  - app/src/index.tsx
  - app/public/index.html
  - app/src/styles.css
  - app/src/routes/root.tsx
autonomous: true
requirements:
  - DSNG-01
  - DSNG-03

must_haves:
  truths:
    - "App renders with dark background (#0f1117) instead of white/gradient"
    - "AppBar renders in dark navy (#1a1a2e), not gold"
    - "Primary buttons render in gold (#c9a227)"
    - "Headings use Josefin Sans font family"
    - "Body text uses Roboto font family"
    - "No hardcoded bgcolor hex values remain in root.tsx"
  artifacts:
    - path: "app/src/theme.ts"
      provides: "MUI createTheme with dark palette, typography, component overrides"
      exports: ["default"]
      contains: "createTheme"
    - path: "app/src/index.tsx"
      provides: "ThemeProvider wiring around app"
      contains: "ThemeProvider"
    - path: "app/public/index.html"
      provides: "Josefin Sans Google Font link"
      contains: "Josefin+Sans"
  key_links:
    - from: "app/src/index.tsx"
      to: "app/src/theme.ts"
      via: "import theme from './theme'"
      pattern: "import theme from"
    - from: "app/src/index.tsx"
      to: "@mui/material/styles"
      via: "ThemeProvider wrapping CssBaseline and app"
      pattern: "ThemeProvider"
---

<objective>
Create the MUI dark theme file and wire it into the app globally. This establishes the design system foundation (color palette, typography, spacing tokens, component overrides) that all subsequent UI work builds against.

Purpose: DSNG-01 (defined visual design language) and DSNG-03 (MUI components used properly) require a centralized theme. Currently the app has no custom MUI theme — raw defaults throughout with one hardcoded bgcolor.

Output: `app/src/theme.ts` with createTheme config; ThemeProvider wiring in index.tsx; Josefin Sans font loaded; gradientBackground retired; root.tsx bgcolor fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-language/02-CONTEXT.md
@.planning/phases/02-design-language/02-RESEARCH.md
@.planning/phases/02-design-language/02-UI-SPEC.md

<interfaces>
<!-- No existing theme infrastructure — this plan creates it from scratch. -->
<!-- Key files the executor needs to understand before modifying: -->

From app/src/index.tsx (current render tree):
```typescript
createRoot(document.getElementById("root") as HTMLElement).render(
    <StrictMode>
        <CssBaseline enableColorScheme />
        <AuthProvider>
            <RouterProvider router={router} />
        </AuthProvider>
    </StrictMode>
);
```

From app/src/routes/root.tsx (line 24 — the hardcoded bgcolor):
```typescript
<Container id="detail" component="main" sx={{ p: 3, width: "90%", bgcolor: "#f0f5fa", mt: 12, mb: 5 }} maxWidth="xl">
```

From app/public/index.html (line 11 — gradientBackground on body):
```html
<body class="gradientBackground">
```

From app/src/styles.css (lines 12-19 — gradientBackground definition):
```css
.gradientBackground {
    background-image:
        radial-gradient(at 51% 52%, rgba(31, 38, 46, 0.5) 0px, transparent 50%),
        radial-gradient(at 80% 0%, rgba(0, 58, 117, 0.3) 0px, transparent 50%),
        radial-gradient(at 0% 95%, rgba(0, 58, 117, 0.5) 0px, transparent 50%),
        radial-gradient(at 0% 5%, rgba(0, 58, 117, 0.5) 0px, transparent 35%),
        radial-gradient(at 93% 85%, rgba(0, 58, 117, 0.3) 0px, transparent 50%);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MUI dark theme file</name>
  <files>app/src/theme.ts</files>
  <read_first>
    - .planning/phases/02-design-language/02-UI-SPEC.md (design contract — palette, typography, component overrides)
    - .planning/phases/02-design-language/02-CONTEXT.md (locked decisions D-01 through D-15)
    - .planning/phases/02-design-language/02-RESEARCH.md (code examples section — exact theme.ts structure)
  </read_first>
  <action>
    Create `app/src/theme.ts` that exports a default `createTheme(...)` result. Import `createTheme` from `"@mui/material/styles"` (NOT from `"@mui/material"` — per Pitfall 5 in RESEARCH.md).

    The theme must contain these exact values per locked decisions:

    **palette** (per D-01 through D-07):
    - `mode: "dark"`
    - `background.default: "#0f1117"` (D-02)
    - `background.paper: "#1a1a2e"` (D-03)
    - `primary.main: "#c9a227"` (D-05)
    - `primary.light: "#e0b83a"` (D-05 hover)
    - `text.primary: "#e8edf5"` (D-04)
    - Do NOT override `error` — use MUI default (D-06)
    - Do NOT override `text.secondary` — MUI dark-mode default opacity applies automatically

    **typography** (per D-08, D-09, D-10):
    - `fontFamily: '"Roboto", sans-serif'` (body default — D-09)
    - `h1` through `h4`: set `fontFamily: '"Josefin Sans", "Roboto", sans-serif'` (D-08)
    - `h6`: set `fontFamily: '"Josefin Sans", "Roboto", sans-serif'`, `fontSize: "20px"`, `fontWeight: 700`, `lineHeight: 1.2` (per UI-SPEC Typography table)
    - `h4`: set `fontFamily: '"Josefin Sans", "Roboto", sans-serif'`, `fontSize: "28px"`, `fontWeight: 700`, `lineHeight: 1.2` (per UI-SPEC Typography table)
    - `body1`: `fontSize: "16px"`, `fontWeight: 400`, `lineHeight: 1.5`
    - `body2`: `fontSize: "14px"`, `fontWeight: 400`, `lineHeight: 1.4`

    **components** (per UI-SPEC Component Overrides table):
    - `MuiButton`: `styleOverrides.root.borderRadius: "6px"`, `defaultProps.disableElevation: true`
    - `MuiAppBar`: `styleOverrides.root.backgroundColor: "#1a1a2e"` (D-07 — matches paper surface)
    - `MuiChip`: `styleOverrides.root.fontFamily: '"Roboto", sans-serif'`
    - `MuiTab`: `styleOverrides.root.textTransform: "none"`
    - Do NOT add MuiDataGrid overrides — it adapts automatically (D-15)
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - File `app/src/theme.ts` exists
    - File contains `import { createTheme } from "@mui/material/styles"`
    - File contains `mode: "dark"`
    - File contains `default: "#0f1117"`
    - File contains `paper: "#1a1a2e"`
    - File contains `main: "#c9a227"`
    - File contains `light: "#e0b83a"`
    - File contains `primary: "#e8edf5"`
    - File contains `"Josefin Sans"`
    - File contains `MuiAppBar`
    - File contains `backgroundColor: "#1a1a2e"`
    - File contains `borderRadius: "6px"`
    - File contains `textTransform: "none"`
    - File contains `export default theme` or `export default createTheme(`
    - TypeScript compiles cleanly (exit 0)
  </acceptance_criteria>
  <done>theme.ts exists, exports a createTheme result with all palette/typography/component values matching the locked decisions, and TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Wire ThemeProvider, load Josefin Sans, retire gradient, fix root bgcolor</name>
  <files>app/src/index.tsx, app/public/index.html, app/src/styles.css, app/src/routes/root.tsx</files>
  <read_first>
    - app/src/index.tsx (current render tree — CssBaseline placement)
    - app/public/index.html (gradientBackground class on body, existing Roboto font link)
    - app/src/styles.css (gradientBackground CSS definition)
    - app/src/routes/root.tsx (hardcoded bgcolor: "#f0f5fa" on Container)
    - .planning/phases/02-design-language/02-RESEARCH.md (Patterns 2, 3 and Anti-Patterns sections)
  </read_first>
  <action>
    **app/src/index.tsx** (per D-13, D-14):
    - Add imports: `import { ThemeProvider } from "@mui/material/styles";` and `import theme from "./theme";`
    - Remove `CssBaseline` from the `@mui/material` import (it will be imported separately — or keep it there, either way is fine)
    - Restructure the render tree so ThemeProvider wraps everything, with CssBaseline INSIDE ThemeProvider:
      ```
      <StrictMode>
        <ThemeProvider theme={theme}>
          <CssBaseline enableColorScheme />
          <AuthProvider>
            <RouterProvider router={router} />
          </AuthProvider>
        </ThemeProvider>
      </StrictMode>
      ```
    - CRITICAL: CssBaseline MUST be inside ThemeProvider or the dark background will not apply (Pitfall 1)

    **app/public/index.html** (per D-11):
    - Add Josefin Sans font link AFTER the existing Roboto link (line 8). Add this line:
      `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@400;700&display=swap" />`
    - The preconnect links for fonts.googleapis.com and fonts.gstatic.com already exist — do NOT duplicate them
    - Change `<body class="gradientBackground">` to just `<body>` — remove the class attribute entirely

    **app/src/styles.css**:
    - Delete the entire `.gradientBackground { ... }` block (lines 12-19). Keep everything else (`body`, `form`, `.match`, `.info-column-item` rules).

    **app/src/routes/root.tsx** (per DSNG-03):
    - On line 24, change `bgcolor: "#f0f5fa"` to `bgcolor: "background.default"` in the Container sx prop. The full sx becomes: `{{ p: 3, width: "90%", bgcolor: "background.default", mt: 12, mb: 5 }}`
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/index.tsx` contains `import { ThemeProvider } from "@mui/material/styles"`
    - `app/src/index.tsx` contains `import theme from "./theme"`
    - `app/src/index.tsx` contains `<ThemeProvider theme={theme}>`
    - `app/src/index.tsx` contains `<CssBaseline` AFTER `<ThemeProvider` (CssBaseline inside ThemeProvider)
    - `app/public/index.html` contains `Josefin+Sans`
    - `app/public/index.html` does NOT contain `class="gradientBackground"`
    - `app/public/index.html` contains `<body>` (no class attribute)
    - `app/src/styles.css` does NOT contain `gradientBackground`
    - `app/src/routes/root.tsx` does NOT contain `#f0f5fa`
    - `app/src/routes/root.tsx` contains `bgcolor: "background.default"`
    - TypeScript compiles cleanly (exit 0)
  </acceptance_criteria>
  <done>ThemeProvider wraps the app with CssBaseline inside it; Josefin Sans loads from Google Fonts CDN; gradientBackground removed from both HTML and CSS; root.tsx uses theme token instead of hardcoded color; TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit` exits 0
2. `app/src/theme.ts` exists and exports a createTheme result
3. No occurrences of `#f0f5fa` or `gradientBackground` in modified files
4. ThemeProvider wraps CssBaseline in index.tsx render tree
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- theme.ts contains all locked palette values (#0f1117, #1a1a2e, #c9a227, #e0b83a, #e8edf5)
- ThemeProvider is the outermost MUI wrapper in the render tree
- Josefin Sans font link present in index.html
- No gradient background artifacts remain
- No hardcoded color hex values in root.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-language/02-01-SUMMARY.md`
</output>
