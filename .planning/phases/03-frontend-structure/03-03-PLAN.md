---
phase: 03-frontend-structure
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/routes/pod/index.tsx
  - app/src/routes/pod/DecksTab.tsx
  - app/src/routes/pod/PlayersTab.tsx
  - app/src/routes/pod/GamesTab.tsx
  - app/src/routes/pod/SettingsTab.tsx
  - app/src/routes/pod.tsx
  - app/src/routes/root.tsx
autonomous: true
requirements:
  - FEND-01
  - DSNG-04
  - FEND-02
must_haves:
  truths:
    - "PodView lives in routes/pod/index.tsx with tab content in separate per-tab files"
    - "Pod tabs use TabbedLayout with queryKey 'podTab'"
    - "DataGrid heights are responsive (xs: 400, sm: 600)"
    - "Promote/Remove buttons have 44px minimum touch target"
    - "Settings form rows wrap on narrow viewports"
    - "AppBar title hidden on xs breakpoint"
  artifacts:
    - path: "app/src/routes/pod/index.tsx"
      provides: "PodView + podLoader using TabbedLayout"
      exports: ["default", "podLoader"]
    - path: "app/src/routes/pod/DecksTab.tsx"
      provides: "Pod Decks tab content"
      exports: ["default"]
    - path: "app/src/routes/pod/PlayersTab.tsx"
      provides: "Pod Players tab content"
      exports: ["default"]
    - path: "app/src/routes/pod/GamesTab.tsx"
      provides: "Pod Games tab content"
      exports: ["default"]
    - path: "app/src/routes/pod/SettingsTab.tsx"
      provides: "Pod Settings tab content"
      exports: ["default"]
  key_links:
    - from: "app/src/routes/pod/index.tsx"
      to: "app/src/components/TabbedLayout.tsx"
      via: "TabbedLayout usage"
      pattern: "import TabbedLayout"
    - from: "app/src/routes/pod/index.tsx"
      to: "app/src/routes/pod/DecksTab.tsx"
      via: "tab content import"
      pattern: "import.*DecksTab"
---

<objective>
Split the monolithic pod.tsx (355 lines) into pod/ subdirectory with per-tab files, migrate to TabbedLayout, and apply Pod-specific UI-SPEC fixes plus the AppBar title mobile fix.

Purpose: Completes FEND-01 for the Pod view, applies TabbedLayout (FEND-02 usage), and fixes mobile usability issues P-01, P-05, P-06, P-07 (DSNG-04).
Output: pod/ subdirectory with 5 files, old pod.tsx deleted, root.tsx AppBar title hidden on mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-structure/03-CONTEXT.md
@.planning/phases/03-frontend-structure/03-RESEARCH.md
@.planning/phases/03-frontend-structure/03-UI-SPEC.md
@.planning/phases/03-frontend-structure/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split pod.tsx into pod/ subdirectory with TabbedLayout migration</name>
  <files>app/src/routes/pod/index.tsx, app/src/routes/pod/DecksTab.tsx, app/src/routes/pod/PlayersTab.tsx, app/src/routes/pod/GamesTab.tsx, app/src/routes/pod/SettingsTab.tsx, app/src/routes/pod.tsx</files>
  <read_first>
    - app/src/routes/pod.tsx (the 355-line monolith to split — read ALL of it)
    - app/src/components/TabbedLayout.tsx (the shared component to use — verify interface)
    - app/src/http.ts (verify function signatures used by pod tabs)
    - app/src/types.ts (verify types used by pod tabs)
    - .planning/phases/03-frontend-structure/03-CONTEXT.md (D-01, D-04 through D-12 for TabbedLayout usage)
  </read_first>
  <action>
Split `app/src/routes/pod.tsx` into 5 files in `app/src/routes/pod/`. Then delete the original `pod.tsx`.

**Import path adjustments** — all tab files are now 2 directories deep from `app/src/`, so:
- `../../auth` for useAuth
- `../../http` for API functions
- `../../types` for type imports
- `../../components/TabbedLayout` for TabbedLayout
- `../../components/stats` for StatColumns

**pod/index.tsx** — Main PodView + podLoader:
- Keep the `podLoader` function (named export)
- Keep the `PodView` function as default export
- Import all 4 tab components: `import PodDecksTab from "./DecksTab"`, etc.
- Replace the `useState(0)` tab state and raw `<Tabs>/<Tab>` rendering with:
  ```tsx
  import TabbedLayout from "../../components/TabbedLayout";

  <TabbedLayout
      queryKey="podTab"
      tabs={[
          { id: "decks", label: "Decks", content: <PodDecksTab decks={decks} podId={pod.id} /> },
          { id: "players", label: "Players", content: <PodPlayersTab players={players} podId={pod.id} isManager={isManager} /> },
          { id: "games", label: "Games", content: <PodGamesTab games={games} podId={pod.id} /> },
          { id: "settings", label: "Settings", content: <PodSettingsTab pod={pod} />, hidden: !isManager },
      ]}
  />
  ```
- Remove `useState` for tab index, remove `handleTabChange`, remove raw Tabs/Tab imports, remove the `{tab === 0 && ...}` conditional rendering
- Keep all data loading logic (useLoaderData, etc.) in index.tsx — pass data as props to tab components

**pod/DecksTab.tsx** — Decks tab content:
- Extract the decks DataGrid rendering into its own component
- Props: receive decks data and podId from parent
- Import StatColumns from `../../components/stats`
- **P-01 fix**: Change DataGrid wrapper Box from `height: 600` to `height: { xs: 400, sm: 600 }`

**pod/PlayersTab.tsx** — Players tab content:
- Extract the players list/grid rendering
- Props: receive players data, podId, isManager flag from parent
- **P-05 fix**: On Promote and Remove `<Button size="small">` elements, add `sx={{ minHeight: 44 }}` for touch targets

**pod/GamesTab.tsx** — Games tab content:
- Extract the games DataGrid rendering
- Props: receive games data and podId from parent
- **P-01 fix**: Change DataGrid wrapper Box from `height: 600` to `height: { xs: 400, sm: 600 }`

**pod/SettingsTab.tsx** — Settings tab content:
- Extract the settings form (pod name edit, invite link generation, etc.)
- Props: receive pod data from parent
- **P-06 fix**: On the TextField + Button flex row, change to `sx={{ display: "flex", gap: 1, flexWrap: "wrap" }}` and add `sx={{ flex: 1, minWidth: 160 }}` to the TextField. Apply same pattern to invite link row.

**Delete original**: Remove `app/src/routes/pod.tsx` after all content has been moved.

Note: The import in `app/src/index.tsx` (`import PodView, { podLoader } from "./routes/pod"`) does NOT need to change — TypeScript resolves `./routes/pod` to `./routes/pod/index.tsx` automatically when the directory has an index file and the old `pod.tsx` flat file is deleted.
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/src/routes/pod/index.tsx exists and contains `export default function PodView` (or similar default export)
    - app/src/routes/pod/index.tsx contains `export` and `podLoader` (named export for loader)
    - app/src/routes/pod/index.tsx contains `import TabbedLayout from "../../components/TabbedLayout"`
    - app/src/routes/pod/index.tsx contains `queryKey="podTab"`
    - app/src/routes/pod/index.tsx contains `hidden: !isManager`
    - app/src/routes/pod/DecksTab.tsx exists and contains `xs: 400` (responsive height)
    - app/src/routes/pod/PlayersTab.tsx exists and contains `minHeight: 44` (touch target fix)
    - app/src/routes/pod/GamesTab.tsx exists and contains `xs: 400` (responsive height)
    - app/src/routes/pod/SettingsTab.tsx exists and contains `flexWrap` (wrap fix)
    - app/src/routes/pod/SettingsTab.tsx contains `minWidth: 160` (TextField min width)
    - app/src/routes/pod.tsx does NOT exist (deleted)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Pod view restructured into pod/ subdirectory with 5 files. TabbedLayout integrated with queryKey "podTab". UI-SPEC fixes P-01, P-05, P-06 applied. Old pod.tsx deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Fix AppBar title visibility on mobile (P-07)</name>
  <files>app/src/routes/root.tsx</files>
  <read_first>
    - app/src/routes/root.tsx (current AppBar — find the "EDH Tracker" Typography)
    - .planning/phases/03-frontend-structure/03-UI-SPEC.md (P-07 specification)
  </read_first>
  <action>
In `app/src/routes/root.tsx`, find the "EDH Tracker" Typography element inside the Toolbar (the one with `variant="h6"` and `letterSpacing: ".3rem"`).

Change its `display` style from `display: "flex"` to `display: { xs: "none", sm: "flex" }`.

This hides the text label on xs (mobile) breakpoints while keeping the playing cards icon visible as the brand anchor. The full sx prop should be:
```tsx
sx={{
    mr: 2,
    display: { xs: "none", sm: "flex" },
    fontWeight: 700,
    letterSpacing: ".3rem",
}}
```

This is the minimal fix for the AppBar crowding issue on 375px viewports where "EDH Tracker" text + PodSelector + Avatar + Logout compete for space.
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/src/routes/root.tsx contains `xs: "none"` (AppBar title hidden on mobile)
    - app/src/routes/root.tsx contains `sm: "flex"` (AppBar title visible on desktop)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>AppBar "EDH Tracker" text hidden on mobile to prevent crowding with PodSelector and user controls. Playing cards icon remains visible as brand anchor.</done>
</task>

</tasks>

<verification>
- `cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit` exits 0
- `ls app/src/routes/pod/` shows: index.tsx, DecksTab.tsx, PlayersTab.tsx, GamesTab.tsx, SettingsTab.tsx
- `ls app/src/routes/pod.tsx 2>&1` shows "No such file"
</verification>

<success_criteria>
- Pod view restructured from 1 monolithic file into 5 per-tab files in pod/ subdirectory
- TabbedLayout integrated with query-string tab persistence (podTab)
- P-01: DataGrid heights responsive (xs: 400, sm: 600)
- P-05: Promote/Remove buttons have 44px minimum touch target
- P-06: Settings form rows wrap on narrow viewports
- P-07: AppBar title hidden on xs breakpoint
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-structure/03-03-SUMMARY.md`
</output>
