---
phase: 03-frontend-structure
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/routes/player/index.tsx
  - app/src/routes/player/OverviewTab.tsx
  - app/src/routes/player/DecksTab.tsx
  - app/src/routes/player/GamesTab.tsx
  - app/src/routes/player/SettingsTab.tsx
  - app/src/routes/player.tsx
autonomous: true
requirements:
  - FEND-01
  - DSNG-04
must_haves:
  truths:
    - "PlayerView lives in routes/player/index.tsx with tab content in per-tab files"
    - "Player tabs use TabbedLayout with queryKey 'playerTab'"
    - "Stats rows use MUI Typography instead of raw span/strong elements"
    - "Error states show fixed user-friendly messages, not raw error.message"
    - "Leave pod buttons have 44px minimum touch target"
  artifacts:
    - path: "app/src/routes/player/index.tsx"
      provides: "PlayerView using TabbedLayout"
      exports: ["default"]
    - path: "app/src/routes/player/OverviewTab.tsx"
      provides: "Player Overview tab with Typography fixes"
      exports: ["default"]
    - path: "app/src/routes/player/DecksTab.tsx"
      provides: "Player Decks tab"
      exports: ["default"]
    - path: "app/src/routes/player/GamesTab.tsx"
      provides: "Player Games tab"
      exports: ["default"]
    - path: "app/src/routes/player/SettingsTab.tsx"
      provides: "Player Settings tab with touch target fixes"
      exports: ["default"]
  key_links:
    - from: "app/src/routes/player/index.tsx"
      to: "app/src/components/TabbedLayout.tsx"
      via: "TabbedLayout usage"
      pattern: "import TabbedLayout"
---

<objective>
Split the monolithic player.tsx (253 lines) into player/ subdirectory with per-tab files, migrate to TabbedLayout, and apply all Player-specific UI-SPEC fixes.

Purpose: Completes FEND-01 for the Player view, uses TabbedLayout (FEND-02), and fixes typography and interaction issues PL-01 through PL-08 (DSNG-04).
Output: player/ subdirectory with 5 files, old player.tsx deleted, all UI-SPEC issues resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-structure/03-CONTEXT.md
@.planning/phases/03-frontend-structure/03-RESEARCH.md
@.planning/phases/03-frontend-structure/03-UI-SPEC.md
@.planning/phases/03-frontend-structure/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split player.tsx into player/ subdirectory with TabbedLayout + UI-SPEC fixes</name>
  <files>app/src/routes/player/index.tsx, app/src/routes/player/OverviewTab.tsx, app/src/routes/player/DecksTab.tsx, app/src/routes/player/GamesTab.tsx, app/src/routes/player/SettingsTab.tsx, app/src/routes/player.tsx</files>
  <read_first>
    - app/src/routes/player.tsx (the 253-line monolith — read ALL of it)
    - app/src/components/TabbedLayout.tsx (verify interface for TabbedLayout usage)
    - app/src/components/common.ts (AsyncComponentHelper used in DecksTab and GamesTab)
    - app/src/types.ts (types used by player view)
    - .planning/phases/03-frontend-structure/03-UI-SPEC.md (PL-01 through PL-08)
  </read_first>
  <action>
Split `app/src/routes/player.tsx` into 5 files in `app/src/routes/player/`. Then delete the original.

**Import path adjustments** for all tab files:
- `../../auth` for useAuth
- `../../http` for API functions
- `../../types` for type imports
- `../../components/TabbedLayout` for TabbedLayout
- `../../components/common` for AsyncComponentHelper
- `../../components/stats` for StatColumns, CommanderColumn, Record
- `../../components/matches` for MatchesDisplay

**player/index.tsx** — Main PlayerView:
- Default export `PlayerView`
- Use `useLoaderData()` to get player data (keep existing loader pattern — `GetPlayer` is used as the loader in index.tsx router config)
- Import all 4 tab components from relative paths
- Replace `useState(0)` tab state with TabbedLayout:
  ```tsx
  <TabbedLayout
      queryKey="playerTab"
      tabs={[
          { id: "overview", label: "Overview", content: <PlayerOverviewTab player={player} /> },
          { id: "decks", label: "Decks", content: <PlayerDecksTab playerId={player.id} /> },
          { id: "games", label: "Games", content: <PlayerGamesTab playerId={player.id} /> },
          { id: "settings", label: "Settings", content: <PlayerSettingsTab player={player} />, hidden: !isOwner },
      ]}
  />
  ```
- Keep the player heading (name + stats) above TabbedLayout
- Remove raw `<Tabs>/<Tab>` rendering and `handleTabChange`

**player/OverviewTab.tsx** — Overview tab:
- Extract the overview section showing player stats, pods list, created-at timestamp
- **PL-01 fix**: Replace `<span><strong>Games Played:</strong> {player.stats.games}</span>` pattern with MUI Typography. Use `<Typography variant="body2" component="span" color="text.secondary">Games Played: </Typography><Typography variant="body1" component="span">{player.stats.games}</Typography>` or a simpler approach: `<Typography variant="body1"><strong>Games Played:</strong> {player.stats.games}</Typography>`
- **PL-02 fix**: On the stats flex row, change to `sx={{ display: "flex", flexDirection: "row", flexWrap: "wrap", gap: 2, px: 1, py: 1 }}` (add `flexWrap: "wrap"`, `gap: 2`, and `px: 1`)
- **PL-04 fix**: Replace `<em>Player created at: {date}</em>` with `<Typography variant="body2" color="text.secondary">Created: {new Date(player.created_at).toLocaleString()}</Typography>`
- **PL-05 fix**: Replace error message `<Typography color="error">Error loading pods: {error.message}</Typography>` with `<Typography variant="body2" color="error">Could not load pods. Refresh to try again.</Typography>` — do NOT expose raw error.message to user

**player/DecksTab.tsx** — Decks tab:
- Extract deck listing using AsyncComponentHelper
- **PL-08 fix**: Replace `return <span>Error Loading Player's Decks: {error.message}</span>` with `return <Box sx={{ p: 2 }}><Typography variant="body2" color="error">Could not load decks. Refresh to try again.</Typography></Box>`
- Add empty state: if data loads but array is empty, show `<Typography variant="body2" color="text.secondary">No decks yet.</Typography>`

**player/GamesTab.tsx** — Games tab:
- Extract games listing using AsyncComponentHelper + MatchesDisplay
- **PL-08 fix**: Replace `return <span>Error Loading Player's Games: {error.message}</span>` with `return <Box sx={{ p: 2 }}><Typography variant="body2" color="error">Could not load games. Refresh to try again.</Typography></Box>`
- Add empty state: if data loads but array is empty, show `<Typography variant="body2" color="text.secondary">No games yet.</Typography>`

**player/SettingsTab.tsx** — Settings tab:
- Extract settings form (player name edit, pod creation, pod leave)
- **PL-07 fix**: On the Leave pod `<Button size="small" color="error">`, add `sx={{ minHeight: 44 }}` for 44px touch target
- **PL-06 note**: The "Create New Pod" section stays in Settings for Phase 3 (POD-01 moves it in Phase 5). Add a `<Divider sx={{ my: 2 }} />` above the "Create New Pod" section.
- Save button label: keep existing label or rename to "Save Name" per copywriting contract if it currently says just "Save"

**Delete original**: Remove `app/src/routes/player.tsx`.

Note: The import in `app/src/index.tsx` (`import PlayerView from "./routes/player"`) resolves to `./routes/player/index.tsx` after the old flat file is deleted.
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/src/routes/player/index.tsx exists and contains `export default` (default export for PlayerView)
    - app/src/routes/player/index.tsx contains `import TabbedLayout from "../../components/TabbedLayout"`
    - app/src/routes/player/index.tsx contains `queryKey="playerTab"`
    - app/src/routes/player/OverviewTab.tsx exists and contains `Typography` (PL-01 fix)
    - app/src/routes/player/OverviewTab.tsx contains `flexWrap` (PL-02 fix)
    - app/src/routes/player/OverviewTab.tsx contains `Could not load pods` (PL-05 fix)
    - app/src/routes/player/OverviewTab.tsx does NOT contain `<em>` (PL-04 fix — removed raw em element)
    - app/src/routes/player/DecksTab.tsx exists and contains `Could not load decks` (PL-08 fix)
    - app/src/routes/player/GamesTab.tsx exists and contains `Could not load games` (PL-08 fix)
    - app/src/routes/player/SettingsTab.tsx exists and contains `minHeight: 44` (PL-07 fix)
    - app/src/routes/player.tsx does NOT exist (deleted)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Player view restructured into player/ subdirectory with 5 files. TabbedLayout integrated with queryKey "playerTab". All UI-SPEC issues PL-01 through PL-08 resolved. Old player.tsx deleted. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit` exits 0
- `ls app/src/routes/player/` shows: index.tsx, OverviewTab.tsx, DecksTab.tsx, GamesTab.tsx, SettingsTab.tsx
- `ls app/src/routes/player.tsx 2>&1` shows "No such file"
</verification>

<success_criteria>
- Player view restructured from 1 monolithic file into 5 per-tab files
- TabbedLayout integrated with queryKey "playerTab"
- PL-01: Stats use Typography not raw span/strong
- PL-02: Stats row wraps on mobile with padding
- PL-04: Timestamp uses Typography body2 not em
- PL-05: Error shows user-friendly message not raw error.message
- PL-07: Leave button has 44px touch target
- PL-08: Error states in DecksTab/GamesTab use Typography
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-structure/03-04-SUMMARY.md`
</output>
