---
phase: 03-frontend-structure
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/routes/login.tsx
  - app/src/routes/root.tsx
  - app/src/components/TooltipIcon.tsx
  - app/src/routes/pod/PlayersTab.tsx
autonomous: true
gap_closure: true
requirements: [DSNG-04]

must_haves:
  truths:
    - "Login page content is positioned with adequate top spacing on desktop -- not vertically centered too low"
    - "Logout control in AppBar renders as an icon button that does not clip on small screens"
    - "TooltipIcon tooltips open above the icon by default"
    - "Promote and Remove buttons in Pod Players tab are contained-style and require confirmation before executing"
  artifacts:
    - path: "app/src/routes/login.tsx"
      provides: "Login page with flex-start positioning and top padding"
      contains: "flex-start"
    - path: "app/src/routes/root.tsx"
      provides: "AppBar with IconButton logout"
      contains: "LogoutIcon"
    - path: "app/src/components/TooltipIcon.tsx"
      provides: "TooltipIcon with placement prop defaulting to top"
      contains: "placement"
    - path: "app/src/routes/pod/PlayersTab.tsx"
      provides: "Contained buttons with confirmation dialogs for Promote and Remove"
      contains: "Dialog"
  key_links:
    - from: "app/src/routes/root.tsx"
      to: "@mui/icons-material/Logout"
      via: "import LogoutIcon"
      pattern: "LogoutIcon"
    - from: "app/src/routes/pod/PlayersTab.tsx"
      to: "@mui/material Dialog"
      via: "confirmation dialog state"
      pattern: "Dialog"
---

<objective>
Apply four UAT-reported UI fixes across the frontend: reposition login page content, replace logout text button with icon, fix tooltip placement direction, and add contained styling plus confirmation dialogs to Pod Players tab actions.

Purpose: Close UAT gaps #4 (cosmetic), #5 (minor), #6 (cosmetic), #8 (major)
Output: Four updated React components passing UAT criteria
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/routes/login.tsx
@app/src/routes/root.tsx
@app/src/components/TooltipIcon.tsx
@app/src/routes/pod/PlayersTab.tsx
</context>

<interfaces>
<!-- Existing patterns the executor needs -->

From app/src/routes/game/index.tsx (confirmation dialog pattern):
```typescript
// State: const [removing, setRemoving] = useState<GameResult | null>(null);
// Trigger: onClick={() => setRemoving(result)}
// Dialog: <Dialog open onClose={...}><DialogTitle>Remove result?</DialogTitle>...
// Confirm: onClick={handleConfirm} then setRemoving(null)
// Cancel: onClick={() => setRemoving(null)}
```

From app/src/components/TooltipIcon.tsx (current interface):
```typescript
interface TooltipIconProps {
    title: string;
    icon?: ReactElement;
}
// No placement prop currently — defaults to MUI Tooltip bottom
```

From app/src/routes/pod/PlayersTab.tsx (current interface):
```typescript
interface PodPlayersTabProps {
    players: PlayerWithRole[];
    podId: number;
    isManager: boolean;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix login positioning, logout icon, and tooltip placement</name>
  <files>app/src/routes/login.tsx, app/src/routes/root.tsx, app/src/components/TooltipIcon.tsx</files>
  <action>
**login.tsx** — Change the outer Box sx from `justifyContent: "center"` to `justifyContent: "flex-start"` and add `pt: { xs: 4, sm: 8 }` to give adequate top spacing without centering content too low on desktop. Keep `minHeight: "100vh"`, `display: "flex"`, `flexDirection: "column"`, and `alignItems: "center"`.

UAT gap: "content starts too low. Fix: change justifyContent from center to flex-start and add top padding/margin on desktop viewports."

**root.tsx** — Replace the text `Button` for logout with an `IconButton` using `Logout` icon from `@mui/icons-material/Logout`. Import `IconButton` from `@mui/material` (already has many imports there) and `LogoutIcon` from `@mui/icons-material/Logout`. The IconButton should:
- Use `color="inherit"` (same as current Button)
- Keep the same `onClick` handler: `() => logout().then(() => navigate("/login"))`
- Wrap in a `Tooltip` with `title="Logout"` so the action is still discoverable
- Import `Tooltip` from `@mui/material`

Remove the text `Button` entirely — do not keep both.

UAT gap: "LOGOUT text clips/crowds on right side of AppBar on small screens."

**TooltipIcon.tsx** — Add an optional `placement` prop to `TooltipIconProps` with type `"top" | "bottom" | "left" | "right"` and default value `"top"`. Pass it through to the MUI `<Tooltip placement={placement}>` element. Also add the same `placement` prop (with same type and default `"top"`) to `TooltipIconButtonProps` and pass it to that component's `<Tooltip>` as well.

UAT gap: "tooltip pops up below the icon. Fix: add placement='top' to the Tooltip."
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <done>
    - Login page uses flex-start with top padding instead of center
    - Logout renders as icon button with tooltip, not text button
    - TooltipIcon and TooltipIconButton both default to placement="top"
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Add contained buttons and confirmation dialogs to Pod PlayersTab</name>
  <files>app/src/routes/pod/PlayersTab.tsx</files>
  <action>
In PlayersTab.tsx, make two changes:

**1. Contained button style:** Add `variant="contained"` to both the Promote and Remove buttons. Keep the existing `size="small"` and `sx={{ minHeight: 44 }}`. The Promote button should use `color="primary"` (default, no change needed). The Remove button already has `color="error"` which will style it as a red contained button.

**2. Confirmation dialogs:** Add confirmation state and Dialog components following the existing pattern from `app/src/routes/game/index.tsx`:

- Add state: `const [confirmAction, setConfirmAction] = useState<{ type: "promote" | "remove"; player: PlayerWithRole } | null>(null);`
- Change Promote `onClick` to: `() => setConfirmAction({ type: "promote", player: p })`
- Change Remove `onClick` to: `() => setConfirmAction({ type: "remove", player: p })`
- Add a single Dialog at the end of the component (outside the List), controlled by `confirmAction !== null`:
  - `<Dialog open={confirmAction !== null} onClose={() => setConfirmAction(null)}>`
  - `<DialogTitle>` shows "Promote player?" or "Remove player?" based on `confirmAction.type`
  - `<DialogContent>` shows a `<DialogContentText>` with contextual message:
    - For promote: `"Promote {player.name} to manager? Managers can edit pod settings and manage members."`
    - For remove: `"Remove {player.name} from this pod? This action cannot be undone."`
  - `<DialogActions>` with Cancel button (`onClick={() => setConfirmAction(null))`) and Confirm button
  - Confirm button: `color="primary"` for promote, `color="error"` for remove
  - Confirm onClick: calls `handlePromote(confirmAction.player.id)` or `handleRemove(confirmAction.player.id)` then `setConfirmAction(null)`

Import `Dialog`, `DialogActions`, `DialogContent`, `DialogContentText`, `DialogTitle` from `@mui/material`.

UAT gap: "Buttons appear as colored text on black background (not contained). Promote and Remove execute immediately with no confirmation dialog."
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <done>
    - Promote and Remove buttons render as contained (filled) buttons
    - Clicking either button opens a confirmation dialog with contextual message
    - Cancel dismisses the dialog without action
    - Confirm executes the action and dismisses the dialog
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
- `cd app && ./node_modules/.bin/tsc --noEmit` passes with no errors
- Visual: Login page content positioned higher on screen with top padding
- Visual: AppBar shows logout icon button (not text) that does not clip on 375px viewport
- Visual: TooltipIcon tooltip appears above the icon
- Visual: Pod Players tab Promote/Remove are filled buttons; clicking opens confirmation dialog
</verification>

<success_criteria>
All four UAT-reported UI issues are resolved. Login repositioned, logout is an icon, tooltips open above, and pod player actions require confirmation via contained buttons.
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-structure/03-08-SUMMARY.md`
</output>
