---
phase: 03-frontend-structure
plan: 06
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/routes/game/index.tsx
  - app/src/routes/game.tsx
  - app/src/routes/new/index.tsx
  - app/src/routes/new.tsx
autonomous: true
requirements:
  - FEND-01
  - DSNG-04
must_haves:
  truths:
    - "GameView lives in routes/game/index.tsx"
    - "NewGameView lives in routes/new/index.tsx"
    - "Game heading uses Typography h4 not raw h1"
    - "Game date uses Typography body2 not raw em"
    - "Edit/Remove icon buttons wrapped with TooltipIconButton"
    - "GameResultsGrid uses autoHeight instead of fixed 355px"
  artifacts:
    - path: "app/src/routes/game/index.tsx"
      provides: "GameView + gameLoader with UI-SPEC fixes"
      exports: ["default", "gameLoader"]
    - path: "app/src/routes/new/index.tsx"
      provides: "NewGameView + newGameLoader + createGame action"
      exports: ["default", "newGameLoader", "createGame"]
  key_links:
    - from: "app/src/routes/game/index.tsx"
      to: "app/src/components/TooltipIcon.tsx"
      via: "TooltipIconButton for edit/remove actions"
      pattern: "import.*TooltipIconButton"
---

<objective>
Move GameView and NewGameView into their own subdirectories and apply all Game-specific UI-SPEC fixes including TooltipIconButton usage.

Purpose: Completes FEND-01 for Game and NewGame views, uses TooltipIconButton (FEND-03), and fixes typography and layout issues G-01 through G-07 (DSNG-04).
Output: game/ and new/ subdirectories, old flat files deleted, Game view polished per UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-structure/03-CONTEXT.md
@.planning/phases/03-frontend-structure/03-RESEARCH.md
@.planning/phases/03-frontend-structure/03-UI-SPEC.md
@.planning/phases/03-frontend-structure/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move GameView to game/ subdirectory with UI-SPEC fixes (G-01 through G-07)</name>
  <files>app/src/routes/game/index.tsx, app/src/routes/game.tsx</files>
  <read_first>
    - app/src/routes/game.tsx (the 410-line file — read ALL of it)
    - app/src/components/TooltipIcon.tsx (verify TooltipIconButton interface for G-03, G-04)
    - .planning/phases/03-frontend-structure/03-UI-SPEC.md (G-01 through G-07)
  </read_first>
  <action>
Move `app/src/routes/game.tsx` to `app/src/routes/game/index.tsx`. Apply all UI-SPEC fixes during the move. Then delete the original.

**Import path adjustments**:
- `../../auth` for useAuth
- `../../http` for API functions
- `../../types` for type imports
- `../../components/TooltipIcon` for TooltipIconButton

**Apply these fixes during the move:**

**G-01 — Typography heading**: Find `<h1>{pod.name} — Game #{game.id}</h1>` (or similar raw h1). Replace with `<Typography variant="h4">{pod.name} — Game #{game.id}</Typography>`. Import `Typography` from `@mui/material` (may already be imported).

**G-02 — Typography date**: Find `<em>{new Date(game.created_at).toLocaleString()}</em>`. Replace with `<Typography variant="body2" color="text.secondary">{new Date(game.created_at).toLocaleString()}</Typography>`.

**G-03 — Edit description button**: Find the `<IconButton size="small" onClick={() => setEditing(true)}><EditIcon /></IconButton>` in GameDescription. Replace with:
```tsx
import { TooltipIconButton } from "../../components/TooltipIcon";

<TooltipIconButton
    title="Edit description"
    icon={<EditIcon fontSize="small" />}
    onClick={() => setEditing(true)}
/>
```
Also verify the description save button says "Save Description" (per copywriting contract — it was updated in Phase 2).

**G-04 — Edit/Remove result buttons**: In GameResultsGrid, find the edit and delete IconButton elements. Replace:
- Edit: `<TooltipIconButton title="Edit result" icon={<EditIcon fontSize="small" />} onClick={...} />`
- Remove/Delete: `<TooltipIconButton title="Remove result" icon={<DeleteIcon fontSize="small" />} onClick={...} />`

**G-05 — Layout alignment**: Find `alignItems: "center"` on the outer GameView Box. Change to `alignItems: "flex-start"` for left-alignment consistent with other views.

**G-06 — DataGrid autoHeight**: Find `<Box sx={{ height: 355, width: "100%" }}>` wrapping the results DataGrid. Remove the fixed height. Change to `<Box sx={{ width: "100%" }}>` and add `autoHeight` prop to the `<DataGrid>`:
```tsx
<Box sx={{ width: "100%" }}>
    <DataGrid
        rows={game.results}
        columns={columns}
        autoHeight
        // ... other existing props
    />
</Box>
```

**G-07 — Delete button margin**: Find `<Button ... sx={{ mt: 2 }}>Delete Game</Button>`. Change `mt: 2` to `mt: 3` for more breathing room above the delete button.

**Delete original**: Remove `app/src/routes/game.tsx`.

Note: The import in `app/src/index.tsx` (`import GameView, { gameLoader } from "./routes/game"`) resolves to `./routes/game/index.tsx` automatically.
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/src/routes/game/index.tsx exists
    - app/src/routes/game/index.tsx contains `variant="h4"` (G-01 fix — no raw h1)
    - app/src/routes/game/index.tsx does NOT contain `<h1>` (G-01 fix — raw h1 removed)
    - app/src/routes/game/index.tsx does NOT contain `<em>` (G-02 fix — raw em removed)
    - app/src/routes/game/index.tsx contains `color="text.secondary"` (G-02 fix)
    - app/src/routes/game/index.tsx contains `TooltipIconButton` (G-03, G-04 fixes)
    - app/src/routes/game/index.tsx contains `Edit description` (G-03 tooltip text)
    - app/src/routes/game/index.tsx contains `Edit result` (G-04 tooltip text)
    - app/src/routes/game/index.tsx contains `Remove result` (G-04 tooltip text)
    - app/src/routes/game/index.tsx contains `alignItems: "flex-start"` (G-05 fix)
    - app/src/routes/game/index.tsx contains `autoHeight` (G-06 fix)
    - app/src/routes/game/index.tsx does NOT contain `height: 355` (G-06 fix — fixed height removed)
    - app/src/routes/game.tsx does NOT exist (deleted)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>GameView moved to game/index.tsx with all 7 UI-SPEC fixes applied. Typography replaces raw HTML elements. TooltipIconButton wraps all icon buttons. autoHeight on DataGrid. Old game.tsx deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Move NewGameView to new/ subdirectory</name>
  <files>app/src/routes/new/index.tsx, app/src/routes/new.tsx</files>
  <read_first>
    - app/src/routes/new.tsx (the 240-line file — read ALL of it)
  </read_first>
  <action>
Move `app/src/routes/new.tsx` to `app/src/routes/new/index.tsx`. This is a straight move with import path updates only — no UI-SPEC fixes needed (NewGameView is excluded from DSNG-04 audit per D-20; Phase 4 redesigns it entirely).

**Import path adjustments** in the moved file:
- Update any `../` imports to `../../` since the file is now one directory deeper:
  - `../auth` becomes `../../auth`
  - `../http` becomes `../../http`
  - `../types` becomes `../../types`
  - Any other relative imports adjusted similarly

Preserve all exports: `default` (NewGameView), `newGameLoader` (named), `createGame` (named).

**Delete original**: Remove `app/src/routes/new.tsx`.

Note: The import in `app/src/index.tsx` (`import NewGameView, { newGameLoader, createGame } from "./routes/new"`) resolves to `./routes/new/index.tsx` automatically.
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/src/routes/new/index.tsx exists
    - app/src/routes/new/index.tsx contains `export default` (NewGameView default export)
    - app/src/routes/new/index.tsx contains `newGameLoader` (named export)
    - app/src/routes/new/index.tsx contains `createGame` (named export)
    - app/src/routes/new.tsx does NOT exist (deleted)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>NewGameView moved to new/index.tsx with updated import paths. All exports preserved. Old new.tsx deleted. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit` exits 0
- `ls app/src/routes/game/index.tsx app/src/routes/new/index.tsx` — both exist
- `ls app/src/routes/game.tsx app/src/routes/new.tsx 2>&1` — both show "No such file"
</verification>

<success_criteria>
- GameView restructured into game/index.tsx with all G-01 through G-07 fixes
- NewGameView moved to new/index.tsx (no content changes, Phase 4 redesigns it)
- TooltipIconButton used for edit/remove actions in GameView
- Typography replaces all raw HTML elements (h1, em)
- autoHeight on GameResultsGrid DataGrid
- Old flat files deleted
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-structure/03-06-SUMMARY.md`
</output>
