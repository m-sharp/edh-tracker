---
phase: 03-frontend-structure
plan: 05
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/routes/deck/index.tsx
  - app/src/routes/deck/OverviewTab.tsx
  - app/src/routes/deck/GamesTab.tsx
  - app/src/routes/deck/SettingsTab.tsx
  - app/src/routes/deck.tsx
autonomous: true
requirements:
  - FEND-01
  - DSNG-04
must_haves:
  truths:
    - "DeckView lives in routes/deck/index.tsx with tab content in per-tab files"
    - "Deck tabs use TabbedLayout with queryKey 'deckTab'"
    - "Stats rows use MUI Typography instead of raw span/strong"
    - "Save buttons labeled 'Save Name', 'Save Format', 'Save Commanders'"
    - "Autocomplete fields use fullWidth not fixed pixel width"
    - "TooltipIcon on Commanders section heading (DECK-02)"
  artifacts:
    - path: "app/src/routes/deck/index.tsx"
      provides: "DeckView using TabbedLayout"
      exports: ["default"]
    - path: "app/src/routes/deck/OverviewTab.tsx"
      provides: "Deck Overview tab with Typography fixes"
      exports: ["default"]
    - path: "app/src/routes/deck/GamesTab.tsx"
      provides: "Deck Games tab"
      exports: ["default"]
    - path: "app/src/routes/deck/SettingsTab.tsx"
      provides: "Deck Settings tab with TooltipIcon and button label fixes"
      exports: ["default"]
  key_links:
    - from: "app/src/routes/deck/index.tsx"
      to: "app/src/components/TabbedLayout.tsx"
      via: "TabbedLayout usage"
      pattern: "import TabbedLayout"
    - from: "app/src/routes/deck/SettingsTab.tsx"
      to: "app/src/components/TooltipIcon.tsx"
      via: "TooltipIcon usage on commander field"
      pattern: "import.*TooltipIcon"
---

<objective>
Split the monolithic deck.tsx (333 lines) into deck/ subdirectory with per-tab files, migrate to TabbedLayout, and apply all Deck-specific UI-SPEC fixes including the TooltipIcon on the commander field (DECK-02).

Purpose: Completes FEND-01 for the Deck view, uses TabbedLayout (FEND-02), uses TooltipIcon (FEND-03), and fixes UI issues D-01 through D-08 (DSNG-04).
Output: deck/ subdirectory with 4 files, old deck.tsx deleted, TooltipIcon on commander section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-structure/03-CONTEXT.md
@.planning/phases/03-frontend-structure/03-RESEARCH.md
@.planning/phases/03-frontend-structure/03-UI-SPEC.md
@.planning/phases/03-frontend-structure/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split deck.tsx into deck/ subdirectory with TabbedLayout + UI-SPEC fixes</name>
  <files>app/src/routes/deck/index.tsx, app/src/routes/deck/OverviewTab.tsx, app/src/routes/deck/GamesTab.tsx, app/src/routes/deck/SettingsTab.tsx, app/src/routes/deck.tsx</files>
  <read_first>
    - app/src/routes/deck.tsx (the 333-line monolith — read ALL of it)
    - app/src/components/TabbedLayout.tsx (verify TabbedLayout interface)
    - app/src/components/TooltipIcon.tsx (verify TooltipIcon interface for D-07)
    - app/src/components/common.ts (AsyncComponentHelper used in GamesTab)
    - app/src/types.ts (types used by deck view)
    - .planning/phases/03-frontend-structure/03-UI-SPEC.md (D-01 through D-08)
    - .planning/phases/03-frontend-structure/03-CONTEXT.md (D-16 — TooltipIcon on commander field)
  </read_first>
  <action>
Split `app/src/routes/deck.tsx` into 4 files in `app/src/routes/deck/`. Then delete the original.

**Import path adjustments** for all tab files:
- `../../auth` for useAuth
- `../../http` for API functions
- `../../types` for type imports
- `../../components/TabbedLayout` for TabbedLayout
- `../../components/common` for AsyncComponentHelper
- `../../components/stats` for Record
- `../../components/matches` for MatchesDisplay
- `../../components/TooltipIcon` for TooltipIcon

**deck/index.tsx** — Main DeckView:
- Default export `DeckView`
- Use `useLoaderData()` to get deck data (loader is `GetDeck` from http.ts, used in index.tsx router config)
- Import all 3 tab components
- Replace `useState(0)` tab state with TabbedLayout:
  ```tsx
  <TabbedLayout
      queryKey="deckTab"
      tabs={[
          { id: "overview", label: "Overview", content: <DeckOverviewTab deck={deck} /> },
          { id: "games", label: "Games", content: <DeckGamesTab deckId={deck.id} commanderName={deck.commander_name} /> },
          { id: "settings", label: "Settings", content: <DeckSettingsTab deck={deck} />, hidden: !isOwner },
      ]}
  />
  ```
- **D-01 fix**: Change deck name heading from `sx={{ mb: 0 }}` to `sx={{ mb: 0.5 }}` for breathing room between deck name and commander sub-heading
- Remove raw `<Tabs>/<Tab>` rendering and tab state

**deck/OverviewTab.tsx** — Overview tab:
- Extract the overview section showing deck stats (games played, kills, points, record)
- **D-02 fix**: Replace `<span><strong>Games Played:</strong> {deck.stats.games}</span>` pattern with `<Typography variant="body1"><strong>Games Played:</strong> {deck.stats.games}</Typography>` — same pattern as player PL-01 fix
- **D-03 fix**: On the stats flex row, change from `gap: 4` to `flexWrap: "wrap", gap: 2` to prevent overflow on 375px viewport

**deck/GamesTab.tsx** — Games tab:
- Extract games listing using AsyncComponentHelper + MatchesDisplay
- Keep existing functionality, update import paths

**deck/SettingsTab.tsx** — Settings tab:
- Extract settings form (deck name edit, format edit, commander edit, retire/delete actions)
- **D-05 fix**: Rename all "Save" buttons to descriptive labels:
  - Name section: `"Save Name"`
  - Format section: `"Save Format"`
  - Commanders section: `"Save Commanders"`
- **D-06 fix**: Replace `sx={{ width: 300 }}` on commander and partner Autocomplete components with `fullWidth` prop. Remove the fixed width entirely. The parent Box's `maxWidth: 500` (if present) handles desktop width.
- **D-07 fix** (DECK-02 + FEND-03): Add TooltipIcon next to the "Commanders" section heading:
  ```tsx
  import { TooltipIcon } from "../../components/TooltipIcon";

  <Box sx={{ display: "flex", alignItems: "center", gap: 0.5 }}>
      <Typography variant="h6">Commanders</Typography>
      <TooltipIcon title="This is for changing an existing deck's commander. To add a new deck, use the Add Deck button instead." />
  </Box>
  ```
- **D-08 fix**: On retire/delete buttons, verify they have adequate touch target. If the buttons use `variant="outlined"` without explicit size, add `sx={{ minHeight: 44 }}` to ensure 44px minimum touch target on mobile.

**Delete original**: Remove `app/src/routes/deck.tsx`.

Note: The import in `app/src/index.tsx` (`import DeckView from "./routes/deck"`) resolves to `./routes/deck/index.tsx` automatically.
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/src/routes/deck/index.tsx exists and contains `export default` (default export for DeckView)
    - app/src/routes/deck/index.tsx contains `import TabbedLayout from "../../components/TabbedLayout"`
    - app/src/routes/deck/index.tsx contains `queryKey="deckTab"`
    - app/src/routes/deck/index.tsx contains `mb: 0.5` (D-01 fix)
    - app/src/routes/deck/OverviewTab.tsx exists and contains `Typography` (D-02 fix)
    - app/src/routes/deck/OverviewTab.tsx contains `flexWrap` (D-03 fix)
    - app/src/routes/deck/GamesTab.tsx exists
    - app/src/routes/deck/SettingsTab.tsx exists and contains `Save Name` (D-05 fix)
    - app/src/routes/deck/SettingsTab.tsx contains `Save Format` (D-05 fix)
    - app/src/routes/deck/SettingsTab.tsx contains `Save Commanders` (D-05 fix)
    - app/src/routes/deck/SettingsTab.tsx contains `fullWidth` (D-06 fix — Autocomplete)
    - app/src/routes/deck/SettingsTab.tsx contains `TooltipIcon` (D-07 fix)
    - app/src/routes/deck/SettingsTab.tsx contains `changing an existing deck` (D-07 tooltip text)
    - app/src/routes/deck/SettingsTab.tsx contains `minHeight: 44` (D-08 fix)
    - app/src/routes/deck.tsx does NOT exist (deleted)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Deck view restructured into deck/ subdirectory with 4 files. TabbedLayout integrated with queryKey "deckTab". All UI-SPEC issues D-01 through D-08 resolved including TooltipIcon on commander field. Old deck.tsx deleted. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit` exits 0
- `ls app/src/routes/deck/` shows: index.tsx, OverviewTab.tsx, GamesTab.tsx, SettingsTab.tsx
- `ls app/src/routes/deck.tsx 2>&1` shows "No such file"
</verification>

<success_criteria>
- Deck view restructured from 1 monolithic file into 4 per-tab files
- TabbedLayout integrated with queryKey "deckTab"
- D-01: Deck name heading spacing fixed (mb: 0.5)
- D-02: Stats use Typography not raw span/strong
- D-03: Stats row wraps on mobile
- D-05: Save buttons labeled "Save Name", "Save Format", "Save Commanders"
- D-06: Autocomplete fields use fullWidth not fixed width
- D-07: TooltipIcon on Commanders heading with DECK-02 text
- D-08: Retire/Delete buttons have 44px touch target
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-structure/03-05-SUMMARY.md`
</output>
