---
phase: 04-game-model-change
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/routes/game/index.tsx
autonomous: true
gap_closure: true
requirements:
  - GAME-01
  - GAME-02

must_haves:
  truths:
    - "AddResultModal deck selector shows '<DeckName> (<PlayerName>)' format"
    - "EditResultModal deck selector shows '<DeckName> (<PlayerName>)' format"
    - "AddResultModal Place field has min=1 and max=playerCount inputProps"
    - "AddResultModal Kills field has min=0 and max=playerCount inputProps"
  artifacts:
    - path: "app/src/routes/game/index.tsx"
      provides: "AddResultModal with playerCount prop, corrected getOptionLabel in both modals"
      contains: "player_name"
  key_links:
    - from: "GameResultsGrid"
      to: "AddResultModal"
      via: "playerCount prop passed as game.results.length + 1"
      pattern: "playerCount.*results\\.length"
---

<objective>
Fix AddResultModal and EditResultModal in GameView: deck selector labels must show player context, and AddResultModal must enforce Place/Kills bounds matching the number of players in the game.

Purpose: UAT gap #1 — the modal deck picker is missing player context and the numeric fields have no input bounds, inconsistent with the NewGameView form.
Output: Updated app/src/routes/game/index.tsx with both modal fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/routes/game/index.tsx
@app/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix deck label and add bounds to AddResultModal and EditResultModal</name>
  <files>app/src/routes/game/index.tsx</files>
  <action>
Two targeted changes in app/src/routes/game/index.tsx:

**Change 1 — Deck label format in both modals**

In `AddResultModal` (line 213): change `getOptionLabel={(d) => d.name}` to `getOptionLabel={(d) => \`${d.name} (${d.player_name})\`}`.

In `EditResultModal` (line 144): change `getOptionLabel={(d) => d.name}` to `getOptionLabel={(d) => \`${d.name} (${d.player_name})\`}`.

The `Deck` type already has `player_name: string` — no import changes needed.

**Change 2 — Place/Kills bounds in AddResultModal**

Add a `playerCount` prop to `AddResultModalProps`:
```typescript
interface AddResultModalProps {
    gameId: number;
    podId: number;
    decks: Deck[];
    playerCount: number;
    onClose: () => void;
}
```

Update the `AddResultModal` function signature to destructure `playerCount`.

Add `inputProps` to the Place TextField:
```tsx
inputProps={{ min: 1, max: playerCount }}
```

Add `inputProps` to the Kills TextField:
```tsx
inputProps={{ min: 0, max: playerCount }}
```

**Change 3 — Pass playerCount from GameResultsGrid**

In `GameResultsGrid`, when rendering `AddResultModal` (around line 346), pass `playerCount={game.results.length + 1}`:
```tsx
<AddResultModal
    gameId={game.id}
    podId={pod.id}
    decks={decks}
    playerCount={game.results.length + 1}
    onClose={() => setAddOpen(false)}
/>
```

The `+1` accounts for the new result being added (current results count + the one being added).

Do NOT add inputProps to EditResultModal — editing an existing result doesn't have the same context for bounding, and that modal was not flagged in the UAT report.
  </action>
  <verify>
    <automated>cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <done>TypeScript compiles clean. AddResultModal getOptionLabel returns "${d.name} (${d.player_name})". EditResultModal getOptionLabel returns "${d.name} (${d.player_name})". AddResultModal Place has inputProps min=1 max=playerCount. AddResultModal Kills has inputProps min=0 max=playerCount. playerCount is passed as game.results.length + 1 from GameResultsGrid.</done>
</task>

</tasks>

<verification>
Run: cd /mnt/d/msharp/Documents/projects/edh-tracker/app && ./node_modules/.bin/tsc --noEmit
Must exit 0 with no errors.
</verification>

<success_criteria>
- TypeScript compiles with no errors
- AddResultModal and EditResultModal both show deck labels as "DeckName (PlayerName)"
- AddResultModal Place field enforces min=1, max=playerCount
- AddResultModal Kills field enforces min=0, max=playerCount
- playerCount is computed dynamically from game.results.length + 1
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-model-change/04-04-SUMMARY.md`
</output>
